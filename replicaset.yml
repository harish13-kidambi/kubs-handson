apiVersion: app/v1
kind: ReplicationController
metadata:
  name: rc-nginx
  labels:
    env: demo
replicas: 4
spec:
  template:
    metadata:
      labels:
        env: demo
    spec:
      containers:
        - name: nginx
          image: nginx:latest